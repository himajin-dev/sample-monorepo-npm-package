{
  "name": "@himajin-dev/sample-monorepo-npm-package-root",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish --no-private --contents dist"
  },
  "devDependencies": {
    "lerna": "^5.6.1",
    "tsc-alias": "^1.7.0",
    "typescript": "^4.8.4"
  }
}
